/** @type {import('next').NextConfig} */
const nextConfig = {
  // When served under ranjan.adsgupta.com/exchange, set basePath: '/exchange'
  // basePath: process.env.NEXT_PUBLIC_BASE_PATH || '',
  reactStrictMode: true,
};

module.exports = nextConfig;
