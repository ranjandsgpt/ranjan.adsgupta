{
  "name": "adsgupta-publisher-tag",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/tag.js --bundle --minify --target=es2018 --outfile=dist/tag.min.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}

